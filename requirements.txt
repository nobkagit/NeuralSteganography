# Primary dependency declarations live in pyproject.toml.
# This file exists for tooling that still expects requirements.txt.
transformers>=4.41.0
tqdm
click>=8.1
pydantic>=2.0
cryptography
numpy
rich
argon2-cffi>=23.1.0
python-dotenv
pytest>=7.0
pytest-cov
mypy
ruff
# Optional GPU extra
# torch
